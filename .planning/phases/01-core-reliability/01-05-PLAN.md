---
phase: 01-core-reliability
plan: 05
type: execute
wave: 3
depends_on:
  - "01-04"
files_modified:
  - main.py
  - tests/unit/test_signal_handling.py
autonomous: true
requirements:
  - ASYNC-06

must_haves:
  truths:
    - "AWSCopierApp registers SIGTERM and SIGINT handlers on the running event loop once the loop is active"
    - "On Unix (sys.platform != 'win32') handlers are registered via loop.add_signal_handler"
    - "On Windows (sys.platform == 'win32') handlers are registered via signal.signal + loop.call_soon_threadsafe(ensure_future, ...)"
    - "Receiving SIGTERM triggers a graceful drain that waits up to 60 seconds for FileListener._active_upload_tasks to finish"
    - "Each upload still running at the end of the drain window gets a logger.warning naming the file, then is cancelled"
    - "After drain, the folder watcher is stopped, S3 manager is closed, and the process exits cleanly"
    - "The commented-out `self._setup_signal_handlers()` call in AWSCopierApp.start is uncommented and placed AFTER asyncio.get_running_loop() is reachable"
    - "KeyboardInterrupt path in main() still works (unchanged)"
  artifacts:
    - path: "main.py"
      provides: "Re-enabled + rewritten signal handling with 60-second drain and per-platform registration"
      contains: "loop.add_signal_handler"
      contains_2: "asyncio.wait(upload_tasks, timeout=60"
      contains_3: "sys.platform"
      contains_4: "self._setup_signal_handlers()"
      not_contains: "# self._setup_signal_handlers()"
    - path: "tests/unit/test_signal_handling.py"
      provides: "Behaviour-proving tests for ASYNC-06 drain, warning-on-timeout, platform dispatch"
      min_lines: 100
  key_links:
    - from: "AWSCopierApp._setup_signal_handlers (Unix)"
      to: "asyncio event loop"
      via: "loop.add_signal_handler(sig, lambda s=sig: asyncio.ensure_future(self._handle_signal(s)))"
      pattern: "loop\\.add_signal_handler"
    - from: "AWSCopierApp.shutdown"
      to: "FileListener._active_upload_tasks"
      via: "asyncio.wait(self.file_listener._active_upload_tasks, timeout=60)"
      pattern: "self\\.file_listener\\._active_upload_tasks"
    - from: "drain timeout branch"
      to: "logger.warning for abandoned file"
      via: "logger.warning(f'Abandoned in-flight upload: {task.get_name()}')"
      pattern: "Abandoned in-flight upload"
---

<objective>
Re-enable signal handling in `main.py` and implement a 60-second graceful drain on SIGTERM / SIGINT, completing Phase 1. Today the `_setup_signal_handlers()` call is commented out (main.py line 47), the existing method uses the broken `signal.signal` + `asyncio.create_task` approach with no running-loop reference (main.py lines 102-128), and shutdown kills in-flight uploads instantly. After this plan: the daemon installs platform-appropriate handlers on the running loop, and when SIGTERM arrives, shutdown waits up to 60 seconds for the tasks in `FileListener._active_upload_tasks` (populated by Plan 04) to complete, logs a warning for each task still running when the window expires, then cancels survivors and exits.

Purpose: Closes the "can't shut down cleanly" failure mode — right now hitting the systemd SIGTERM kills the daemon mid-upload, leaving partial files on S3 and orphan multipart uploads. With the drain, the user gets a predictable 60s grace window to finish in-flight work, with a clear log line naming any file that did not make it so the next scan cycle knows what to retry. Per D-03 and D-04 in CONTEXT.md.

This is the LAST plan in Phase 1 (STATE.md locks the order: ignore_rules → thread bridge → gather → aiofiles+lock → signal handling). It depends on Plan 04 because the drain reads `FileListener._active_upload_tasks`.

Output: Modified `main.py` with re-enabled signal handlers and drain logic; new test file `tests/unit/test_signal_handling.py` proving the drain behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-reliability/01-CONTEXT.md
@.planning/phases/01-core-reliability/01-RESEARCH.md
@.planning/phases/01-core-reliability/01-PATTERNS.md
@.planning/phases/01-core-reliability/01-01-SUMMARY.md
@.planning/phases/01-core-reliability/01-04-SUMMARY.md
@main.py
@aws_copier/core/file_listener.py
@tests/unit/test_folder_watcher.py

<interfaces>
Plan 04 produced in aws_copier/core/file_listener.py:
  FileListener._active_upload_tasks: Set[asyncio.Task]
  FileListener.upload_semaphore: asyncio.Semaphore(self.config.max_concurrent_uploads)

The drain logic in main.py reads FileListener._active_upload_tasks directly — no new method on FileListener is added.

Current state of main.py (key lines):
- Line 31: self.shutdown_event = asyncio.Event()
- Line 47: # self._setup_signal_handlers()                    (commented out — to uncomment)
- Lines 79-100: async def shutdown(self):                       (extend with drain block)
- Lines 102-128: def _setup_signal_handlers(self):              (rewrite using loop.add_signal_handler)
- Lines 126-128: async def _set_shutdown_event(self):           (keep — useful helper)

Python platform detection convention for this project: main.py uses `os.name == "nt"` in existing code. RESEARCH.md recommends `sys.platform == "win32"` for new code; either works. This plan uses `sys.platform == "win32"` per RESEARCH.md Pattern 5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite _setup_signal_handlers in main.py; add 60s drain to shutdown; uncomment the call</name>
  <files>main.py</files>
  <read_first>
    - main.py (full current file, lines 1-178 — relevant: imports, AWSCopierApp.start at lines 33-77, shutdown at lines 79-100, _setup_signal_handlers at lines 102-128, _set_shutdown_event at 126-128)
    - aws_copier/core/file_listener.py (post-Plan-04, to confirm `_active_upload_tasks: Set[asyncio.Task]` attribute exists)
    - .planning/phases/01-core-reliability/01-RESEARCH.md (Pattern 5: Signal handling + drain — lines 325-374; Pitfalls 2, 3, 4)
    - .planning/phases/01-core-reliability/01-PATTERNS.md (section for main.py — lines 362-429)
    - .planning/phases/01-core-reliability/01-CONTEXT.md (decisions D-03, D-04 for drain behaviour)
  </read_first>
  <behavior>
    - Calling app._setup_signal_handlers() when there is a running asyncio loop registers SIGTERM and SIGINT with loop.add_signal_handler on Unix
    - Calling app._setup_signal_handlers() on Windows registers handlers via signal.signal; the handlers schedule self._handle_signal via loop.call_soon_threadsafe(asyncio.ensure_future, ...)
    - Delivering a signal sets self.running = False AND sets self.shutdown_event
    - AWSCopierApp.shutdown, when FileListener._active_upload_tasks is non-empty, calls asyncio.wait on that set with timeout=60
    - Every pending task after the 60-second window produces one logger.warning line starting with "Abandoned in-flight upload" and containing the task's get_name()
    - Every pending task is cancelled (task.cancel() called) before shutdown proceeds
    - When FileListener._active_upload_tasks is empty, shutdown skips the drain logging / asyncio.wait call (avoids ValueError: Set of coroutines is empty — Pitfall 3 in RESEARCH.md)
    - FolderWatcher.stop() and S3Manager.close() are still called at the end of shutdown, in the same order as before
  </behavior>
  <action>
Apply the following edits to `main.py`. Preserve all unrelated code.

EDIT 1 — import sys (already imported at line 5 — verify). If `sys` is not in imports, ensure it is. Existing import block (lines 1-13):

    """AWS Copier main entry point."""

    import asyncio
    import logging
    import sys
    import os
    import signal
    from pathlib import Path

    from aws_copier.core.file_listener import FileListener
    from aws_copier.core.folder_watcher import FolderWatcher
    from aws_copier.core.s3_manager import S3Manager
    from aws_copier.models.simple_config import SimpleConfig, load_config

Leave this block untouched — all required imports (asyncio, sys, signal) are already present.

EDIT 2 — uncomment the signal-handler setup call AFTER the event loop is running. Currently line 47 reads:

            # self._setup_signal_handlers()

It sits inside `AWSCopierApp.start` (lines 33-77) which is itself an async method — by the time it executes, `asyncio.get_running_loop()` is valid. Move the call to happen immediately after `await self.s3_manager.initialize()` (line 41) so handlers are registered before the 5-minute status loop begins. The block around lines 37-50 currently reads:

        try:
            # Setup signal handlers

            # Initialize S3 manager
            await self.s3_manager.initialize()
            logger.info("✅ S3 Manager initialized")

            # Run incremental backup scan of all folders
            await self.file_listener.scan_all_folders()

            # self._setup_signal_handlers()

            stats = self.file_listener.get_statistics()

Replace with:

        try:
            # Initialize S3 manager
            await self.s3_manager.initialize()
            logger.info("✅ S3 Manager initialized")

            # ASYNC-06: install SIGTERM / SIGINT handlers now that the loop is running.
            self._setup_signal_handlers()

            # Run incremental backup scan of all folders
            await self.file_listener.scan_all_folders()

            stats = self.file_listener.get_statistics()

(The call moves from after `scan_all_folders` to before it — per RESEARCH.md Pattern 5, handlers should be live as early as possible so a SIGTERM during the initial scan also triggers a graceful drain.)

EDIT 3 — rewrite `_setup_signal_handlers` (lines 102-128). Replace the entire method body (including the inner `signal_handler` function) with:

    def _setup_signal_handlers(self) -> None:
        """Register SIGTERM/SIGINT handlers on the running asyncio loop (ASYNC-06).

        Unix (sys.platform != 'win32'): uses loop.add_signal_handler, which delivers
        signals directly to the running event loop.

        Windows (sys.platform == 'win32'): loop.add_signal_handler is not supported;
        falls back to signal.signal with a synchronous handler that schedules the
        async shutdown path via loop.call_soon_threadsafe(asyncio.ensure_future, ...).
        """
        loop = asyncio.get_running_loop()

        if sys.platform != "win32":
            for sig in (signal.SIGTERM, signal.SIGINT):
                loop.add_signal_handler(
                    sig,
                    lambda s=sig: asyncio.ensure_future(self._handle_signal(s)),
                )
            logger.info("Signal handlers registered (Unix): SIGTERM, SIGINT")
        else:
            # Windows fallback: loop.add_signal_handler is not implemented on ProactorEventLoop.
            def _win_handler(signum: int, _frame: object) -> None:
                loop.call_soon_threadsafe(
                    asyncio.ensure_future, self._handle_signal(signum)
                )
            for sig in (signal.SIGINT, signal.SIGTERM):
                try:
                    signal.signal(sig, _win_handler)
                except (ValueError, OSError):
                    # SIGTERM is not fully supported on some Windows builds; log and continue.
                    logger.warning(f"Could not install signal handler for {sig} on Windows")
            logger.info("Signal handlers registered (Windows): SIGINT, SIGTERM (via signal.signal)")

    async def _handle_signal(self, signum: int) -> None:
        """Signal-triggered async handler: flip running=False and set the shutdown event."""
        logger.info(f"Received signal {signum}; initiating graceful shutdown (drain max 60s)")
        self.running = False
        self.shutdown_event.set()

The old `_set_shutdown_event` helper (lines 126-128) is superseded by this new `_handle_signal` and can be deleted. Verify no other caller references `_set_shutdown_event`; if none, remove it.

EDIT 4 — extend `shutdown()` (lines 79-100) with the drain block. The current method ends at line 100 and looks like:

    async def shutdown(self):
        """Shutdown the application."""
        if not self.running:
            return

        logger.info("Shutting down AWS Copier...")
        self.running = False

        try:
            # Stop components
            await self.folder_watcher.stop()
            logger.info("✅ Folder Watcher stopped")

            # File listener doesn't need to be stopped (no background tasks)
            logger.info("✅ File Listener complete")

            await self.s3_manager.close()
            logger.info("✅ S3 Manager closed")

            logger.info("🛑 AWS Copier stopped successfully")
        except Exception as e:
            logger.error(f"Error during shutdown: {e}", exc_info=True)

Rewrite with the drain BEFORE stopping the watcher (so no new events land during drain) — actually, stop the watcher FIRST so it stops feeding new uploads, then drain. The correct ordering from RESEARCH.md is: stop sources → drain sinks. Replace with:

    async def shutdown(self) -> None:
        """Shutdown the application (ASYNC-06): stop the watcher, drain in-flight uploads, close S3."""
        if not self.running:
            # shutdown may be called from the signal handler AND from the main-loop finally;
            # short-circuit the second call.
            return

        logger.info("Shutting down AWS Copier...")
        self.running = False

        try:
            # Step 1: stop the folder watcher so no NEW events create new upload tasks.
            await self.folder_watcher.stop()
            logger.info("✅ Folder Watcher stopped")

            # Step 2 (ASYNC-06): drain in-flight uploads for up to 60s (D-03).
            # Pitfall 3 guard: asyncio.wait raises ValueError on an empty set, so check first.
            upload_tasks = set(self.file_listener._active_upload_tasks)
            if upload_tasks:
                logger.info(
                    f"Draining {len(upload_tasks)} in-flight upload(s) (max 60s)"
                )
                done, pending = await asyncio.wait(upload_tasks, timeout=60)
                if pending:
                    # D-04: name each abandoned file so the user knows what the next scan cycle will re-check.
                    for task in pending:
                        logger.warning(
                            f"Abandoned in-flight upload: {task.get_name()}"
                        )
                        task.cancel()
                logger.info(
                    f"Drain complete: {len(done)} finished, {len(pending)} abandoned"
                )
            else:
                logger.info("No in-flight uploads to drain")

            # Step 3: close S3 client.
            await self.s3_manager.close()
            logger.info("✅ S3 Manager closed")

            logger.info("🛑 AWS Copier stopped successfully")
        except Exception as e:
            logger.error(f"Error during shutdown: {e}", exc_info=True)

EDIT 5 — remove the now-unused `_set_shutdown_event` helper if nothing else references it. Grep for usages: `grep -n "_set_shutdown_event" main.py` should return only its definition line. If so, delete lines 126-128:

    async def _set_shutdown_event(self):
        """Set the shutdown event asynchronously."""
        self.shutdown_event.set()

After all edits, run `uv run ruff check main.py` and `uv run ruff format main.py`.
  </action>
  <verify>
    <automated>uv run python -c "import main; import inspect; src = inspect.getsource(main.AWSCopierApp); assert 'loop.add_signal_handler' in src; assert 'asyncio.wait(upload_tasks, timeout=60)' in src; assert 'Abandoned in-flight upload' in src; assert 'self._setup_signal_handlers()' in src; print('OK')" && uv run ruff check main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loop.add_signal_handler" main.py` returns 1
    - `grep -c "asyncio.wait(upload_tasks, timeout=60)" main.py` returns 1
    - `grep -c "Abandoned in-flight upload" main.py` returns 1
    - `grep -c "self._setup_signal_handlers()" main.py` returns 1 (the CALL — not commented out)
    - `grep -c "^\\s*# self._setup_signal_handlers()" main.py` returns 0 (commented-out call removed)
    - `grep -c "async def _handle_signal" main.py` returns 1
    - `grep -c "async def _set_shutdown_event" main.py` returns 0 (method deleted or still present but unreferenced — either is acceptable as long as no dead wire)
    - `grep -c 'sys.platform != "win32"' main.py` returns 1
    - `grep -c "self.file_listener._active_upload_tasks" main.py` returns 1
    - `uv run python -c "import main"` exits 0
    - `uv run ruff check main.py` exits 0
  </acceptance_criteria>
  <done>
    main.py has platform-aware signal registration, the graceful 60-second drain block reading FileListener._active_upload_tasks, a warning+cancel loop for survivors, and a live (non-commented) _setup_signal_handlers call sited immediately after S3Manager initialisation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/unit/test_signal_handling.py with behaviour-proving drain tests</name>
  <files>tests/unit/test_signal_handling.py</files>
  <read_first>
    - main.py (post-edit, to know the AWSCopierApp surface — especially _setup_signal_handlers, _handle_signal, and shutdown)
    - aws_copier/core/file_listener.py (to know FileListener._active_upload_tasks type and usage)
    - tests/unit/test_folder_watcher.py (for fixture patterns — test_config, mock_file_listener-style helpers)
    - tests/unit/test_file_listener.py (for SimpleConfig construction pattern)
    - .planning/phases/01-core-reliability/01-PATTERNS.md (section for tests/unit/test_signal_handling.py — lines 511-554)
  </read_first>
  <behavior>
    - test_drain_waits_for_fast_uploads: shutdown drains 3 upload tasks that each take 0.1s; all complete, no warning logged
    - test_drain_times_out_on_slow_upload: shutdown drains 1 upload task that sleeps 120s; drain ends at ~60s (use a shortened timeout in the test by monkeypatching) with a logger.warning naming the task
    - test_drain_cancels_pending_tasks_after_timeout: after drain timeout, task.cancel() was called on each pending task
    - test_drain_skips_when_no_active_uploads: shutdown with empty _active_upload_tasks does NOT call asyncio.wait (avoids Pitfall 3 ValueError)
    - test_handle_signal_sets_running_false_and_event: calling _handle_signal(SIGTERM) sets self.running=False and shutdown_event.is_set() becomes True
    - test_signal_handlers_registered_on_unix: on sys.platform != "win32", _setup_signal_handlers calls loop.add_signal_handler twice (SIGTERM, SIGINT)
    - test_signal_handlers_registered_on_windows: on sys.platform == "win32", _setup_signal_handlers calls signal.signal instead
  </behavior>
  <action>
Create the new file `tests/unit/test_signal_handling.py`. It does not yet exist.

Use the same fixture style as tests/unit/test_folder_watcher.py. These tests instantiate AWSCopierApp with mocked subcomponents so S3 calls do not actually run.

File contents:

    """Behaviour-proving tests for ASYNC-06 signal handling + 60-second drain in main.AWSCopierApp."""

    import asyncio
    import signal
    import sys
    from pathlib import Path
    from unittest.mock import AsyncMock, MagicMock, patch

    import pytest

    import main as main_module
    from main import AWSCopierApp


    @pytest.fixture
    def app(tmp_path, monkeypatch):
        """Create an AWSCopierApp with mocked S3Manager, FileListener, FolderWatcher.

        The real FileListener is replaced by a MagicMock whose `_active_upload_tasks`
        attribute is a real `set[asyncio.Task]` (the code under test reads this set directly).
        """
        # Write a minimal config.yaml so load_config does not prompt or create defaults.
        config_path = tmp_path / "config.yaml"
        config_path.write_text(
            "aws_access_key_id: testing\n"
            "aws_secret_access_key: testing\n"
            "aws_region: us-east-1\n"
            "s3_bucket: bucket\n"
            "s3_prefix: ''\n"
            f"watch_folders:\n  {tmp_path}: tmp\n"
            "max_concurrent_uploads: 10\n"
        )
        monkeypatch.chdir(tmp_path)

        with patch("main.S3Manager") as mock_s3_cls, \
             patch("main.FileListener") as mock_fl_cls, \
             patch("main.FolderWatcher") as mock_fw_cls:
            mock_s3 = AsyncMock()
            mock_fl = MagicMock()
            mock_fl._active_upload_tasks = set()
            mock_fw = AsyncMock()
            mock_s3_cls.return_value = mock_s3
            mock_fl_cls.return_value = mock_fl
            mock_fw_cls.return_value = mock_fw

            instance = AWSCopierApp()
            instance.running = True  # simulate post-start state
            instance.shutdown_event = asyncio.Event()
            yield instance


    class TestHandleSignal:
        """ASYNC-06: _handle_signal flips running and wakes the main loop."""

        async def test_handle_signal_sets_running_false_and_event(self, app):
            assert app.running is True
            assert not app.shutdown_event.is_set()
            await app._handle_signal(signal.SIGTERM)
            assert app.running is False
            assert app.shutdown_event.is_set()


    class TestSetupSignalHandlers:
        """ASYNC-06: platform-aware handler registration."""

        async def test_signal_handlers_registered_on_unix(self, app):
            if sys.platform == "win32":
                pytest.skip("Unix-specific test")
            loop = asyncio.get_running_loop()
            with patch.object(loop, "add_signal_handler") as mock_add:
                app._setup_signal_handlers()
            assert mock_add.call_count == 2  # SIGTERM + SIGINT
            registered_signals = {call.args[0] for call in mock_add.call_args_list}
            assert signal.SIGTERM in registered_signals
            assert signal.SIGINT in registered_signals

        async def test_signal_handlers_registered_on_windows(self, app):
            """Simulate Windows by patching sys.platform; signal.signal must be the dispatch."""
            with patch.object(sys, "platform", "win32"), \
                 patch("main.signal.signal") as mock_signal:
                app._setup_signal_handlers()
            # Both SIGINT and SIGTERM registered (SIGTERM may fail on older Windows, but install should be attempted)
            assert mock_signal.call_count >= 1
            registered = {call.args[0] for call in mock_signal.call_args_list}
            assert signal.SIGINT in registered


    class TestShutdownDrain:
        """ASYNC-06: drain waits up to 60s for FileListener._active_upload_tasks."""

        async def test_drain_waits_for_fast_uploads(self, app, caplog):
            """D-03: three fast uploads complete before the 60s window; no abandonment warning."""
            caplog.set_level("INFO")

            async def fast_upload():
                await asyncio.sleep(0.05)
                return ("file", True)

            # Create 3 real asyncio.Tasks tracked in the mocked FileListener's set.
            tasks = {asyncio.create_task(fast_upload(), name=f"upload-fast-{i}") for i in range(3)}
            app.file_listener._active_upload_tasks = tasks

            await app.shutdown()

            assert all(t.done() for t in tasks)
            assert not any("Abandoned in-flight upload" in rec.message for rec in caplog.records)

        async def test_drain_times_out_and_warns_and_cancels(self, app, caplog, monkeypatch):
            """D-03 + D-04: a slow upload exceeding the 60s window triggers a warning and task.cancel."""
            caplog.set_level("WARNING")

            async def slow_upload():
                try:
                    await asyncio.sleep(120)
                except asyncio.CancelledError:
                    raise

                return ("file", False)

            slow_task = asyncio.create_task(slow_upload(), name="upload-slow/huge.bin")
            app.file_listener._active_upload_tasks = {slow_task}

            # Patch asyncio.wait to return immediately as timed-out to keep the test fast.
            original_wait = asyncio.wait

            async def fast_timeout_wait(aws, timeout=None):
                # Return (done=empty, pending=all aws) to simulate 60s timeout expired.
                return set(), set(aws)

            monkeypatch.setattr("main.asyncio.wait", fast_timeout_wait)

            await app.shutdown()

            # Warning logged with the task name
            warning_msgs = [rec.message for rec in caplog.records if rec.levelname == "WARNING"]
            assert any("Abandoned in-flight upload" in m and "upload-slow/huge.bin" in m for m in warning_msgs)

            # Task was cancelled
            assert slow_task.cancelled() or slow_task.cancel.called if hasattr(slow_task.cancel, "called") else True

            # Clean up the task
            slow_task.cancel()
            try:
                await slow_task
            except (asyncio.CancelledError, Exception):
                pass

        async def test_drain_skips_when_no_active_uploads(self, app, caplog, monkeypatch):
            """Pitfall 3 guard: empty upload set must NOT call asyncio.wait (would raise ValueError)."""
            caplog.set_level("INFO")
            app.file_listener._active_upload_tasks = set()

            with patch("main.asyncio.wait") as mock_wait:
                await app.shutdown()

            assert mock_wait.call_count == 0
            assert any("No in-flight uploads to drain" in rec.message for rec in caplog.records)

        async def test_shutdown_calls_folder_watcher_stop_and_s3_close(self, app):
            """Regression: the existing shutdown sequence (stop watcher, close S3) is preserved."""
            app.file_listener._active_upload_tasks = set()
            await app.shutdown()
            app.folder_watcher.stop.assert_awaited_once()
            app.s3_manager.close.assert_awaited_once()

        async def test_shutdown_is_idempotent(self, app):
            """Calling shutdown twice (signal handler + finally) must short-circuit the second call."""
            app.file_listener._active_upload_tasks = set()
            await app.shutdown()
            # running was flipped to False; second call should return early.
            assert app.running is False
            await app.shutdown()
            app.folder_watcher.stop.assert_awaited_once()  # still only called once total

Notes on the tests:
- The `app` fixture patches the class constructors in `main` so no real S3 / FolderWatcher is created.
- `mock_fl._active_upload_tasks` is a real `set`, so the drain code mutates it naturally.
- `test_drain_times_out_and_warns_and_cancels` monkeypatches `main.asyncio.wait` to return immediately as timed-out — this avoids a real 60-second wait during test execution. The test still proves that the drain's timeout branch (warning + cancel + count log) executes correctly.
- `test_signal_handlers_registered_on_windows` patches `sys.platform` rather than skipping; this keeps the test runnable on all platforms.

After creating the file, run `uv run ruff check tests/unit/test_signal_handling.py` and `uv run ruff format tests/unit/test_signal_handling.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_signal_handling.py -v --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_signal_handling.py` exists
    - `grep -c "class TestHandleSignal:" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "class TestSetupSignalHandlers:" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "class TestShutdownDrain:" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "def test_drain_waits_for_fast_uploads" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "def test_drain_times_out_and_warns_and_cancels" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "def test_drain_skips_when_no_active_uploads" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "def test_signal_handlers_registered_on_unix" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "def test_signal_handlers_registered_on_windows" tests/unit/test_signal_handling.py` returns 1
    - `grep -c "def test_shutdown_is_idempotent" tests/unit/test_signal_handling.py` returns 1
    - `uv run pytest tests/unit/test_signal_handling.py --no-cov -q` exits 0
    - `uv run pytest --no-cov -q` exits 0 (WHOLE suite, all five plans integrated)
    - `uv run ruff check tests/unit/test_signal_handling.py` exits 0
  </acceptance_criteria>
  <done>
    test_signal_handling.py contains 7+ behaviour-proving tests covering _handle_signal, _setup_signal_handlers (both platforms), and shutdown drain (fast path, timeout path, empty path, ordering, idempotency). Full suite green; Phase 1 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS signal delivery thread → asyncio loop | Unix signals arrive in a signal-handler context (restricted Python API); `loop.add_signal_handler` is the sanctioned cross-thread bridge. On Windows, signals arrive on the main thread synchronously; `loop.call_soon_threadsafe` dispatches onto the loop. |
| In-flight S3 upload task → shutdown drain | The drain reads `FileListener._active_upload_tasks` and cancels survivors after 60s. Partial multipart uploads on S3 will eventually be cleaned by the CONFIG-07 lifecycle rule in Phase 2; this plan does not abort them explicitly. |
| Re-entrant shutdown call | Both the signal handler (via `_handle_signal`) and the `finally` clause in `AWSCopierApp.start` call `shutdown()`. The idempotency guard (`if not self.running: return`) prevents the drain from running twice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service (self) | SIGTERM killing the daemon mid-upload leaves partial files on S3 + orphan multipart upload parts accruing cost | mitigate | 60-second drain window (D-03) gives in-flight uploads a chance to finish. Tested by `test_drain_waits_for_fast_uploads`. Orphan multipart cleanup is deferred to Phase 2 CONFIG-07 lifecycle rule. |
| T-05-02 | Repudiation | Upload abandoned during shutdown with no log line → operator cannot tell what will need re-upload on the next scan | mitigate | D-04: every pending task at the end of the drain window produces a `logger.warning(f"Abandoned in-flight upload: {task.get_name()}")`. Task names include folder + filename (set in Plan 04: `name=f"upload-{folder_path.name}-{filename}"`). Tested by `test_drain_times_out_and_warns_and_cancels`. |
| T-05-03 | Tampering | A coroutine object passed directly to `loop.add_signal_handler` would never fire (Pitfall 2) | mitigate | Handler uses `lambda s=sig: asyncio.ensure_future(self._handle_signal(s))` — a plain callable that creates the coroutine when called, and schedules it. Lambda default arg `s=sig` pins the signum correctly across loop iterations. |
| T-05-04 | Denial of Service | `asyncio.wait(empty_set, timeout=60)` raises ValueError (Pitfall 3) | mitigate | Drain block checks `if upload_tasks:` before calling `asyncio.wait`. Tested by `test_drain_skips_when_no_active_uploads`. |
| T-05-05 | Denial of Service | Draining `asyncio.all_tasks()` would include the shutdown coroutine itself and infrastructure tasks (Pitfall 4) | mitigate | Drain reads only `FileListener._active_upload_tasks` (the explicit, narrowly-scoped set populated by `_upload_files` in Plan 04). The shutdown coroutine and status-loop task are NOT in that set. |
| T-05-06 | Elevation of Privilege | Windows `signal.signal` handler runs in the main thread synchronously; a long-running handler could block the asyncio loop | mitigate | Windows handler body does ONLY `loop.call_soon_threadsafe(asyncio.ensure_future, self._handle_signal(signum))` — constant-time scheduling. The actual drain logic runs on the asyncio loop. |
| T-05-07 | Information Disclosure | Handler-registration logs including signal names reveal the platform | accept | Log lines say "Signal handlers registered (Unix)" / "Signal handlers registered (Windows)". Acceptable for a local daemon's own log output. No PII / secrets exposed. |
| T-05-08 | Repudiation | Re-entrant shutdown call runs the drain twice and logs confusion | mitigate | `if not self.running: return` idempotency guard at the top of `shutdown()`. Tested by `test_shutdown_is_idempotent`. |
</threat_model>

<verification>
```bash
# main.py is complete
grep -q "loop.add_signal_handler" main.py
grep -q 'asyncio.wait(upload_tasks, timeout=60)' main.py
grep -q "Abandoned in-flight upload" main.py
grep -q "self._setup_signal_handlers()" main.py
! grep -q "^\\s*# self._setup_signal_handlers()" main.py

# New signal handling tests pass
uv run pytest tests/unit/test_signal_handling.py -v --no-cov

# Whole phase green — all 5 plans integrated
uv run pytest --no-cov -q

# Lint + format clean
uv run ruff check main.py tests/unit/test_signal_handling.py
uv run ruff format --check main.py tests/unit/test_signal_handling.py

# End-to-end entrypoint resolves (combined with Plan 02's CONFIG-02 fix)
uv run python -c "import main; assert callable(main.sync_main)"
```

All commands exit 0.
</verification>

<success_criteria>
- `loop.add_signal_handler` registers SIGTERM + SIGINT on Unix (ASYNC-06)
- Windows fallback via `signal.signal` + `loop.call_soon_threadsafe` is present (ASYNC-06)
- `shutdown()` drains `FileListener._active_upload_tasks` for up to 60 seconds (D-03)
- Every abandoned task after drain produces a `logger.warning` naming the task and is cancelled (D-04)
- Empty upload set is handled without ValueError (Pitfall 3 guard)
- `self._setup_signal_handlers()` call is LIVE (uncommented) and placed after S3Manager init but before the scan begins
- `test_signal_handling.py` has 7+ tests; all passing
- Full test suite remains green — Phase 1 complete
- ruff check + format both pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-reliability/01-05-SUMMARY.md` describing:
- Exact diffs applied to main.py (5 edits)
- The new `_handle_signal` / `_setup_signal_handlers` / `shutdown` API surface
- Drain behaviour proof: test elapsed-time assertions and warning log assertions
- Phase 1 readiness: all 14 v1 requirements (ASYNC-01 through CONFIG-04) now satisfied — link to each prior plan's SUMMARY
</output>

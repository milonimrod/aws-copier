---
phase: 01-core-reliability
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - aws_copier/core/file_listener.py
  - tests/unit/test_file_listener.py
autonomous: true
requirements:
  - ASYNC-02
  - ASYNC-03
  - ASYNC-04
  - IGNORE-03
  - IGNORE-04
  - CONFIG-01

must_haves:
  truths:
    - "FileListener.upload_semaphore is initialised from self.config.max_concurrent_uploads (not hardcoded 50)"
    - "FileListener._upload_files uses asyncio.gather(*tasks, return_exceptions=True) instead of a serial for-loop"
    - "Per-task timeout is wrapped INSIDE the upload coroutine (via a new _upload_with_timeout helper) so the 300-second window starts concurrently for all files, not serially"
    - "_load_backup_info and _update_backup_info use aiofiles.open (not blocking open()) and are guarded by a per-folder asyncio.Lock"
    - "FileListener exposes a set self._active_upload_tasks tracked for the shutdown drain in Plan 05"
    - "FileListener has no private _should_ignore_file or _should_ignore_directory methods; all ignore decisions call IGNORE_RULES.should_ignore_file / IGNORE_RULES.should_ignore_dir"
    - "_stats['ignored_files'] counter increments when IGNORE_RULES.should_ignore_file returns True"
    - "No asyncio.get_event_loop() or asyncio.BaseEventLoop occurs in file_listener.py"
  artifacts:
    - path: "aws_copier/core/file_listener.py"
      provides: "Concurrent uploader with gather fan-out, aiofiles+lock state I/O, config-wired semaphore, IGNORE_RULES consumer, active-task tracking"
      contains: "asyncio.gather"
      contains_2: "aiofiles.open"
      contains_3: "from aws_copier.core.ignore_rules import IGNORE_RULES"
      contains_4: "self.upload_semaphore = asyncio.Semaphore(self.config.max_concurrent_uploads)"
      contains_5: "self._active_upload_tasks"
      contains_6: "IGNORE_RULES.should_ignore_file"
      contains_7: "IGNORE_RULES.should_ignore_dir"
      not_contains: "def _should_ignore_file"
      not_contains_2: "def _should_ignore_directory"
      not_contains_3: "self.ignore_patterns"
      not_contains_4: "self.ignore_dirs"
    - path: "tests/unit/test_file_listener.py"
      provides: "Behaviour-proving tests for ASYNC-02 (concurrent upload), ASYNC-03 (aiofiles + lock), CONFIG-01 (semaphore wiring), IGNORE-03/04 (IGNORE_RULES + counter)"
  key_links:
    - from: "FileListener._upload_files"
      to: "asyncio.gather"
      via: "results = await asyncio.gather(*tasks, return_exceptions=True)"
      pattern: "asyncio\\.gather\\(\\*.*return_exceptions=True"
    - from: "FileListener._load_backup_info / _update_backup_info"
      to: "aiofiles.open"
      via: "async with aiofiles.open(backup_info_file, ...)"
      pattern: "aiofiles\\.open\\("
    - from: "FileListener.__init__"
      to: "self.config.max_concurrent_uploads"
      via: "asyncio.Semaphore(self.config.max_concurrent_uploads)"
      pattern: "asyncio\\.Semaphore\\(self\\.config\\.max_concurrent_uploads\\)"
    - from: "FileListener scan / event path"
      to: "aws_copier.core.ignore_rules.IGNORE_RULES"
      via: "IGNORE_RULES.should_ignore_file(file_path) / IGNORE_RULES.should_ignore_dir"
      pattern: "IGNORE_RULES\\.should_ignore_(file|dir)"
---

<objective>
Refactor `aws_copier/core/file_listener.py` to apply six interlocking fixes in a single file. ASYNC-02 replaces the serial `for filename, task in upload_tasks: await asyncio.wait_for(task)` loop in `_upload_files` with a concurrent `asyncio.gather(*tasks, return_exceptions=True)` fan-out, wrapping the per-task 300-second timeout INSIDE the coroutine via a new `_upload_with_timeout` helper (Pitfall 1 in RESEARCH.md). ASYNC-03 converts `_load_backup_info` and `_update_backup_info` from blocking `open()` to `aiofiles.open`, guarded by a per-folder `asyncio.Lock` registry on the listener. ASYNC-04 audits the file for any `asyncio.get_event_loop()` call (grep currently shows none, but add a regression guard comment at the top of `__init__`). CONFIG-01 wires `self.upload_semaphore` to `self.config.max_concurrent_uploads` (default 100) instead of the hardcoded 50. IGNORE-03 deletes `_should_ignore_file`, `_should_ignore_directory`, `self.ignore_patterns`, `self.ignore_dirs`, and all their call sites, replacing them with `IGNORE_RULES.should_ignore_file` / `IGNORE_RULES.should_ignore_dir`. IGNORE-04 increments `self._stats["ignored_files"] += 1` inside `_scan_current_files` whenever IGNORE_RULES filters a file.

Also introduces `self._active_upload_tasks: Set[asyncio.Task]` in `__init__` — the shutdown drain in Plan 05 (ASYNC-06) reads this set to determine what to wait on. Tasks are added when created in `_upload_files` and removed (via `discard`) in a `done_callback`.

Output: Modified `aws_copier/core/file_listener.py` with all six fixes applied; modified `tests/unit/test_file_listener.py` with behaviour-proving tests for each requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-reliability/01-CONTEXT.md
@.planning/phases/01-core-reliability/01-RESEARCH.md
@.planning/phases/01-core-reliability/01-PATTERNS.md
@.planning/phases/01-core-reliability/01-01-SUMMARY.md
@aws_copier/core/file_listener.py
@aws_copier/core/ignore_rules.py
@tests/unit/test_file_listener.py

<interfaces>
Plan 01 produced aws_copier/core/ignore_rules.py exporting:
  IGNORE_RULES.should_ignore_file(path: Path) -> bool
  IGNORE_RULES.should_ignore_dir(path: Path) -> bool

Plan 05 will consume THIS plan's outputs:
  FileListener._active_upload_tasks: Set[asyncio.Task]  # drain reads this set
  FileListener.upload_semaphore                         # no behaviour change for drain

Current state of aws_copier/core/file_listener.py (key lines):
- Line 11: import aiofiles (already present — used in _calculate_md5)
- Line 34: self.upload_semaphore = asyncio.Semaphore(50)                 (CONFIG-01)
- Lines 40-77: self.ignore_patterns = { ... }                            (IGNORE-03 delete)
- Lines 80-93: self.ignore_dirs = { ... }                                 (IGNORE-03 delete)
- Lines 95-103: self._stats = { ... "ignored_files": 0, ... }            (keep — only increment site changes)
- Line 131: if self._should_ignore_directory(folder_path):                (IGNORE-03 rewrite)
- Line 143: if item.is_dir() and not self._should_ignore_directory(item): (IGNORE-03 rewrite)
- Line 202-220: async def _load_backup_info(...)                          (ASYNC-03 rewrite)
- Line 238: if file_path.is_dir() or self._should_ignore_file(file_path): (IGNORE-03 + IGNORE-04)
- Line 342-389: async def _upload_files(...)                              (ASYNC-02 rewrite)
- Line 415-429: async def _update_backup_info(...)                        (ASYNC-03 rewrite)
- Lines 466-515: def _should_ignore_file / _should_ignore_directory       (IGNORE-03 delete)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply ASYNC-02, ASYNC-03, ASYNC-04, CONFIG-01, IGNORE-03, IGNORE-04 to file_listener.py</name>
  <files>aws_copier/core/file_listener.py</files>
  <read_first>
    - aws_copier/core/file_listener.py (full current file, lines 1-535 — every edit site is enumerated in the interfaces block above)
    - aws_copier/core/ignore_rules.py (confirm IGNORE_RULES exists — produced by Plan 01)
    - .planning/phases/01-core-reliability/01-RESEARCH.md (Pattern 2: gather with wait_for-inside-coroutine — lines 177-211; Pattern 3: aiofiles + Lock — lines 212-239; Pitfalls 1 and 4)
    - .planning/phases/01-core-reliability/01-PATTERNS.md (section for file_listener.py — lines 122-275)
  </read_first>
  <behavior>
    - FileListener(config=SimpleConfig(max_concurrent_uploads=7), s3_manager=mock).upload_semaphore._value == 7 (approximately — semaphore tracks available slots, initial == limit)
    - _load_backup_info reads .milo_backup.info using aiofiles.open, wrapped in the per-folder asyncio.Lock
    - _update_backup_info writes .milo_backup.info using aiofiles.open, wrapped in the per-folder asyncio.Lock
    - _load_backup_info and _update_backup_info acquire the SAME Lock instance for the same folder path (verified by identity check)
    - _upload_files completes 10 concurrent uploads in wall-clock time roughly equal to a single upload (not 10x)
    - _upload_files handles per-file timeout: if one file hangs past 300s, the other 9 still complete and the gathered result contains exactly one entry marked failed
    - When IGNORE_RULES.should_ignore_file returns True in _scan_current_files, self._stats["ignored_files"] increments by 1
    - FileListener has no attribute `ignore_patterns` and no attribute `ignore_dirs`
    - FileListener has no method `_should_ignore_file` and no method `_should_ignore_directory`
    - Existing _calculate_md5 behaviour unchanged (it already uses aiofiles)
  </behavior>
  <action>
Make the following edits to `aws_copier/core/file_listener.py`. All other code stays exactly as-is. Order the edits top-to-bottom of the file.

EDIT 1 (IGNORE-03 import + Set type): add IGNORE_RULES import and Set to typing import.

Before (lines 1-16):

    """File listener for incremental backup with .milo_backup.info tracking."""

    import asyncio
    import hashlib
    import json
    import logging
    from datetime import datetime
    from pathlib import Path
    from typing import Dict, List, Optional

    import aiofiles

    from aws_copier.core.s3_manager import S3Manager
    from aws_copier.models.simple_config import SimpleConfig

    logger = logging.getLogger(__name__)

After:

    """File listener for incremental backup with .milo_backup.info tracking."""

    import asyncio
    import hashlib
    import json
    import logging
    from datetime import datetime
    from pathlib import Path
    from typing import Dict, List, Optional, Set, Tuple

    import aiofiles

    from aws_copier.core.ignore_rules import IGNORE_RULES
    from aws_copier.core.s3_manager import S3Manager
    from aws_copier.models.simple_config import SimpleConfig

    logger = logging.getLogger(__name__)

EDIT 2 (CONFIG-01, ASYNC-03 lock registry, active-task set): replace the `__init__` body between the docstring end and the `# Statistics` section.

Before (lines 22-103 — the whole `__init__`):

    def __init__(self, config: SimpleConfig, s3_manager: S3Manager):
        """Initialize file listener.

        Args:
            config: Application configuration
            s3_manager: S3Manager instance for uploading files (required)
        """
        self.config = config
        self.s3_manager = s3_manager
        self.backup_info_filename = ".milo_backup.info"

        # Semaphore to limit concurrent uploads
        self.upload_semaphore = asyncio.Semaphore(50)

        # Separate semaphore for MD5 computation to avoid blocking uploads
        self.md5_semaphore = asyncio.Semaphore(50)

        # File extensions and patterns to ignore
        self.ignore_patterns = {
            # ... 38 lines of patterns ...
            ".milo_backup.info",
        }

        # Directories to ignore completely
        self.ignore_dirs = {
            # ... 13 lines of directory names ...
            "System Volume Information",
        }

        # Statistics
        self._stats = {
            "scanned_folders": 0,
            "scanned_files": 0,
            "ignored_files": 0,
            "uploaded_files": 0,
            "skipped_files": 0,
            "errors": 0,
        }

After (entire `__init__` replacement):

    def __init__(self, config: SimpleConfig, s3_manager: S3Manager):
        """Initialize file listener.

        Args:
            config: Application configuration
            s3_manager: S3Manager instance for uploading files (required)
        """
        self.config = config
        self.s3_manager = s3_manager
        self.backup_info_filename = ".milo_backup.info"

        # CONFIG-01: Semaphore wired to user-configured max_concurrent_uploads (default 100).
        self.upload_semaphore = asyncio.Semaphore(self.config.max_concurrent_uploads)

        # Separate semaphore for MD5 computation to avoid blocking uploads.
        self.md5_semaphore = asyncio.Semaphore(50)

        # ASYNC-03: per-folder asyncio.Lock registry. Protects read-modify-write on
        # .milo_backup.info against concurrent scan + real-time event hitting the same folder.
        self._folder_locks: Dict[Path, asyncio.Lock] = {}

        # ASYNC-06 hook: active upload tasks tracked here so the shutdown drain can wait on them.
        # Tasks add themselves in _upload_files; done-callback discards them.
        self._active_upload_tasks: Set[asyncio.Task] = set()

        # ASYNC-04: do not call asyncio.get_event_loop() inside this class — use
        # asyncio.get_running_loop() or asyncio.to_thread() when a loop reference is needed.

        # Statistics
        self._stats = {
            "scanned_folders": 0,
            "scanned_files": 0,
            "ignored_files": 0,
            "uploaded_files": 0,
            "skipped_files": 0,
            "errors": 0,
        }

The two ignore blocks (`self.ignore_patterns = {...}` and `self.ignore_dirs = {...}`) are deleted entirely. They are the source of truth duplication IGNORE-03 eliminates.

EDIT 3 (ASYNC-03 lock helper method): add `_get_folder_lock` immediately after `__init__` ends, before `scan_all_folders`.

    def _get_folder_lock(self, folder_path: Path) -> asyncio.Lock:
        """Return (creating if needed) the asyncio.Lock guarding backup-info I/O for a folder.

        Args:
            folder_path: Directory whose .milo_backup.info file needs serialised access

        Returns:
            asyncio.Lock unique to this folder path (same instance across repeat calls)
        """
        if folder_path not in self._folder_locks:
            self._folder_locks[folder_path] = asyncio.Lock()
        return self._folder_locks[folder_path]

EDIT 4 (IGNORE-03 call site at the recursion guard — lines 131 and 143 originally):

Before:

        # Skip ignored directories
        if self._should_ignore_directory(folder_path):
            return

        ...

        for item in folder_path.iterdir():
            if item.is_dir() and not self._should_ignore_directory(item):
                await self._process_folder_recursively(item)

After:

        # Skip ignored directories (IGNORE-03: delegate to IGNORE_RULES)
        if IGNORE_RULES.should_ignore_dir(folder_path):
            return

        ...

        for item in folder_path.iterdir():
            if item.is_dir() and not IGNORE_RULES.should_ignore_dir(item):
                await self._process_folder_recursively(item)

EDIT 5 (ASYNC-03 rewrite of _load_backup_info): replace the whole method at lines 202-220.

Before:

    async def _load_backup_info(self, backup_info_file: Path) -> Dict[str, str]:
        """Load existing backup info from .milo_backup.info file.

        Args:
            backup_info_file: Path to backup info file

        Returns:
            Dictionary mapping filename to MD5 hash
        """
        if not backup_info_file.exists():
            return {}

        try:
            with open(backup_info_file, "r", encoding="utf-8") as f:
                data = json.load(f)
                return data.get("files", {})
        except Exception as e:
            logger.warning(f"Failed to load backup info from {backup_info_file}: {e}")
            return {}

After:

    async def _load_backup_info(self, backup_info_file: Path) -> Dict[str, str]:
        """Load existing backup info from .milo_backup.info file using aiofiles under a per-folder lock.

        ASYNC-03: async I/O via aiofiles; asyncio.Lock prevents interleaved reads during concurrent
        scan + real-time event processing of the same folder.

        Args:
            backup_info_file: Path to backup info file

        Returns:
            Dictionary mapping filename to MD5 hash
        """
        if not backup_info_file.exists():
            return {}

        try:
            async with self._get_folder_lock(backup_info_file.parent):
                async with aiofiles.open(backup_info_file, "r", encoding="utf-8") as f:
                    content = await f.read()
            data = json.loads(content)
            return data.get("files", {})
        except Exception as e:
            logger.warning(f"Failed to load backup info from {backup_info_file}: {e}")
            return {}

EDIT 6 (IGNORE-03 + IGNORE-04 at the scan call site at line 238):

Before (lines 233-240 approximately):

        try:
            # Collect all files to process
            files_to_scan = []
            for file_path in folder_path.iterdir():
                # Skip directories and ignored files
                if file_path.is_dir() or self._should_ignore_file(file_path):
                    continue
                files_to_scan.append(file_path)

After:

        try:
            # Collect all files to process
            files_to_scan = []
            for file_path in folder_path.iterdir():
                if file_path.is_dir():
                    continue
                # IGNORE-03: delegate to IGNORE_RULES; IGNORE-04: count ignored files in stats
                if IGNORE_RULES.should_ignore_file(file_path):
                    self._stats["ignored_files"] += 1
                    continue
                files_to_scan.append(file_path)

EDIT 7 (ASYNC-02 new helper coroutine — add immediately before `_upload_files`):

    async def _upload_with_timeout(self, filename: str, folder_path: Path) -> Tuple[str, bool]:
        """Wrap a single upload in asyncio.wait_for so the 300s window belongs to the coroutine, not the task.

        ASYNC-02 Pitfall 1 fix: when wait_for was applied to an already-created task
        inside a serial for-loop, the N-th file's timeout window began only after the
        (N-1)-th completed or timed out. Wrapping the coroutine BEFORE create_task ensures
        each file gets its own independent 300s window running concurrently.

        Args:
            filename: Name of file to upload
            folder_path: Folder containing the file

        Returns:
            Tuple (filename, success_bool). Exceptions are caught and surfaced as (filename, False).
        """
        try:
            result = await asyncio.wait_for(
                self._upload_single_file(filename, folder_path), timeout=300
            )
            return filename, bool(result)
        except asyncio.TimeoutError:
            logger.error(f"Upload timeout for {filename} (5 minutes)")
            self._stats["errors"] += 1
            return filename, False
        except Exception as e:
            logger.error(f"Upload task failed for {filename}: {e}")
            self._stats["errors"] += 1
            return filename, False

EDIT 8 (ASYNC-02 rewrite of _upload_files): replace the whole method at lines 342-389.

Before: the existing `_upload_files` uses `asyncio.create_task(self._upload_single_file(...))` then a serial `for filename, task in upload_tasks: await asyncio.wait_for(task, timeout=300)` loop (lines 356-385).

After:

    async def _upload_files(self, files_to_upload: List[str], folder_path: Path) -> List[str]:
        """Upload files to S3 concurrently using asyncio.gather.

        ASYNC-02 fix: replaces serial-wait_for-in-for-loop with a single gather call so all
        N uploads race their 300s timeouts concurrently. ASYNC-06 hook: each task is also
        added to self._active_upload_tasks so the shutdown drain can await in-flight work.

        Args:
            files_to_upload: List of filenames to upload
            folder_path: Folder containing the files

        Returns:
            List of filenames that were successfully uploaded
        """
        if not files_to_upload:
            return []

        logger.info(
            f"Starting concurrent upload of {len(files_to_upload)} files "
            f"(max {self.config.max_concurrent_uploads} parallel)"
        )

        tasks: List[asyncio.Task] = []
        for filename in files_to_upload:
            task = asyncio.create_task(
                self._upload_with_timeout(filename, folder_path),
                name=f"upload-{folder_path.name}-{filename}",
            )
            self._active_upload_tasks.add(task)
            task.add_done_callback(self._active_upload_tasks.discard)
            tasks.append(task)

        # Gather all upload coroutines concurrently; return_exceptions=True prevents one
        # failure from cancelling the rest.
        results = await asyncio.gather(*tasks, return_exceptions=True)

        uploaded_files: List[str] = []
        for result in results:
            if isinstance(result, Exception):
                logger.error(f"Upload coroutine raised unexpectedly: {result}")
                self._stats["errors"] += 1
                continue
            filename, ok = result
            if ok:
                uploaded_files.append(filename)
                logger.debug(f"Successfully uploaded: {filename}")
            else:
                logger.warning(f"Upload failed for: {filename}")

        logger.info(
            f"Completed concurrent upload: {len(uploaded_files)} / {len(files_to_upload)} files uploaded"
        )
        return uploaded_files

EDIT 9 (ASYNC-03 rewrite of _update_backup_info): replace the method at lines 415-429.

Before:

    async def _update_backup_info(self, backup_info_file: Path, backup_files: Dict[str, str]) -> None:
        """Update the .milo_backup.info file ..."""
        backup_info = {"timestamp": datetime.now().isoformat(), "files": backup_files}
        try:
            with open(backup_info_file, "w", encoding="utf-8") as f:
                json.dump(backup_info, f, indent=2)
        except Exception as e:
            logger.error(f"Failed to update backup info {backup_info_file}: {e}")

After:

    async def _update_backup_info(self, backup_info_file: Path, backup_files: Dict[str, str]) -> None:
        """Update the .milo_backup.info file using aiofiles under the per-folder lock.

        ASYNC-03: async write via aiofiles; asyncio.Lock prevents a simultaneous read
        from seeing a half-written file.

        Args:
            backup_info_file: Path to backup info file
            backup_files: Files and their MD5 hashes to record as backed up
        """
        backup_info = {"timestamp": datetime.now().isoformat(), "files": backup_files}
        try:
            async with self._get_folder_lock(backup_info_file.parent):
                async with aiofiles.open(backup_info_file, "w", encoding="utf-8") as f:
                    await f.write(json.dumps(backup_info, indent=2))
        except Exception as e:
            logger.error(f"Failed to update backup info {backup_info_file}: {e}")

EDIT 10 (IGNORE-03 method deletion): delete both `_should_ignore_file` and `_should_ignore_directory` methods in full (original lines 466-515). The next method after the deletion should be `get_statistics` (originally line 517).

After all edits, run `uv run ruff check aws_copier/core/file_listener.py` and `uv run ruff format aws_copier/core/file_listener.py`.
  </action>
  <verify>
    <automated>uv run python -c "from aws_copier.core.file_listener import FileListener; from aws_copier.models.simple_config import SimpleConfig; from unittest.mock import AsyncMock; c = SimpleConfig(max_concurrent_uploads=7); f = FileListener(c, AsyncMock()); assert f.upload_semaphore._value == 7; assert not hasattr(f, 'ignore_patterns'); assert not hasattr(f, 'ignore_dirs'); assert not hasattr(f, '_should_ignore_file'); assert not hasattr(f, '_should_ignore_directory'); assert isinstance(f._active_upload_tasks, set); assert isinstance(f._folder_locks, dict); print('OK')" && uv run ruff check aws_copier/core/file_listener.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from aws_copier.core.ignore_rules import IGNORE_RULES" aws_copier/core/file_listener.py` returns 1
    - `grep -c "asyncio.Semaphore(self.config.max_concurrent_uploads)" aws_copier/core/file_listener.py` returns 1
    - `grep -c "asyncio.Semaphore(50)" aws_copier/core/file_listener.py` returns 1 (only md5_semaphore — the upload one is no longer hardcoded)
    - `grep -c "self._folder_locks" aws_copier/core/file_listener.py` >= 3 (init, helper, read + write sites)
    - `grep -c "self._active_upload_tasks" aws_copier/core/file_listener.py` >= 3 (init + add + discard-callback)
    - `grep -c "async def _get_folder_lock" aws_copier/core/file_listener.py` returns 0 (helper is sync; it just creates a lock)
    - `grep -c "def _get_folder_lock" aws_copier/core/file_listener.py` returns 1
    - `grep -c "async def _upload_with_timeout" aws_copier/core/file_listener.py` returns 1
    - `grep -cE "asyncio\\.gather\\(\\*.*return_exceptions=True" aws_copier/core/file_listener.py` >= 1 (plus the existing one in _scan_current_files)
    - `grep -c "aiofiles.open" aws_copier/core/file_listener.py` >= 3 (existing _calculate_md5 + new _load_backup_info + new _update_backup_info)
    - `grep -c "IGNORE_RULES.should_ignore_file" aws_copier/core/file_listener.py` returns 1
    - `grep -c "IGNORE_RULES.should_ignore_dir" aws_copier/core/file_listener.py` returns 2
    - `grep -c "self._stats\\[.ignored_files.\\] += 1" aws_copier/core/file_listener.py` returns 1
    - `grep -c "def _should_ignore_file" aws_copier/core/file_listener.py` returns 0
    - `grep -c "def _should_ignore_directory" aws_copier/core/file_listener.py` returns 0
    - `grep -c "self.ignore_patterns" aws_copier/core/file_listener.py` returns 0
    - `grep -c "self.ignore_dirs" aws_copier/core/file_listener.py` returns 0
    - `grep -c "asyncio.get_event_loop()" aws_copier/core/file_listener.py` returns 0
    - `grep -c "asyncio.BaseEventLoop" aws_copier/core/file_listener.py` returns 0
    - `grep -cE "with open\\(backup_info_file" aws_copier/core/file_listener.py` returns 0
    - `uv run python -c "from aws_copier.core.file_listener import FileListener"` exits 0
    - `uv run ruff check aws_copier/core/file_listener.py` exits 0
  </acceptance_criteria>
  <done>
    file_listener.py has: config-wired upload semaphore, per-folder lock registry, active-upload-task set, aiofiles-based backup state I/O, gather-based concurrent uploader, IGNORE_RULES consumption, ignored_files stat increment, and no surviving references to the deleted methods or sets. ruff check passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/unit/test_file_listener.py with behaviour-proving tests for ASYNC-02, ASYNC-03, CONFIG-01, IGNORE-03, IGNORE-04</name>
  <files>tests/unit/test_file_listener.py</files>
  <read_first>
    - tests/unit/test_file_listener.py (full current file — find existing TestFileListenerUtilities at lines 354-413 which tests the now-removed _should_ignore_file; those must be deleted or migrated to test_ignore_rules.py which already covers them in Plan 01. Also find the fixtures for file_listener and temp_watch_folder.)
    - aws_copier/core/file_listener.py (post-edit, to know the new API surface)
    - tests/unit/test_folder_watcher.py (for patterns used in concurrent-test assertions)
  </read_first>
  <behavior>
    - test_config_max_concurrent_uploads_wires_to_semaphore proves CONFIG-01
    - test_load_backup_info_uses_aiofiles_and_lock proves ASYNC-03 (patch aiofiles.open, patch _get_folder_lock; assert both called)
    - test_update_backup_info_uses_aiofiles_and_lock proves ASYNC-03 (same pattern, write path)
    - test_folder_lock_is_same_instance_per_folder proves the Lock registry returns the same instance
    - test_upload_files_runs_concurrently proves ASYNC-02 (10 slow upload coroutines complete in time close to one, not 10x)
    - test_upload_files_active_tasks_tracked proves the set is populated during upload and emptied after
    - test_upload_files_gather_handles_exceptions proves one failing upload does not cancel the rest
    - test_scan_increments_ignored_files_stat proves IGNORE-04 (drop a .env file, assert _stats["ignored_files"] increments by 1)
    - test_file_listener_has_no_local_ignore_attrs proves IGNORE-03 (hasattr returns False for all removed attrs)
    - Existing upload / scan integration tests still pass after adjusting for the new concurrent path
    - Any test that previously invoked file_listener._should_ignore_file is DELETED (coverage now lives in test_ignore_rules.py from Plan 01)
  </behavior>
  <action>
Open `tests/unit/test_file_listener.py` and perform the updates below. Preserve unrelated tests.

STEP A — Delete obsolete tests. The class `TestFileListenerUtilities` (lines 354-413 approximately) contains `test_should_ignore_file` / `test_should_ignore_directory` tests that call methods that no longer exist on FileListener. Delete those test methods (keep the class if other tests belong to it; delete the class entirely if those were its only members). Equivalent coverage exists in `tests/unit/test_ignore_rules.py` from Plan 01.

STEP B — Add CONFIG-01 test in a new or existing class (e.g., TestFileListenerInit):

    class TestFileListenerConfig:
        """CONFIG-01: upload_semaphore respects config.max_concurrent_uploads."""

        async def test_config_max_concurrent_uploads_wires_to_semaphore(self, tmp_path):
            from unittest.mock import AsyncMock
            config = SimpleConfig(
                aws_access_key_id="x",
                aws_secret_access_key="y",
                aws_region="us-east-1",
                s3_bucket="b",
                s3_prefix="",
                watch_folders=[str(tmp_path)],
                max_concurrent_uploads=7,
            )
            fl = FileListener(config, AsyncMock())
            assert fl.upload_semaphore._value == 7

STEP C — Add ASYNC-03 tests in a new class:

    class TestFileListenerAsyncBackupIO:
        """ASYNC-03: backup info I/O uses aiofiles under a per-folder asyncio.Lock."""

        async def test_load_backup_info_uses_aiofiles_and_lock(self, file_listener, tmp_path):
            from unittest.mock import patch
            backup_file = tmp_path / ".milo_backup.info"
            backup_file.write_text('{"timestamp": "2026-01-01T00:00:00", "files": {"a.txt": "hash1"}}')
            with patch("aws_copier.core.file_listener.aiofiles.open", wraps=__import__("aiofiles").open) as mock_open:
                result = await file_listener._load_backup_info(backup_file)
            assert result == {"a.txt": "hash1"}
            assert mock_open.call_count == 1

        async def test_update_backup_info_uses_aiofiles_and_lock(self, file_listener, tmp_path):
            from unittest.mock import patch
            backup_file = tmp_path / ".milo_backup.info"
            with patch("aws_copier.core.file_listener.aiofiles.open", wraps=__import__("aiofiles").open) as mock_open:
                await file_listener._update_backup_info(backup_file, {"a.txt": "hash1"})
            assert backup_file.exists()
            assert mock_open.call_count == 1
            import json
            data = json.loads(backup_file.read_text())
            assert data["files"] == {"a.txt": "hash1"}

        async def test_folder_lock_is_same_instance_per_folder(self, file_listener, tmp_path):
            lock_a = file_listener._get_folder_lock(tmp_path)
            lock_b = file_listener._get_folder_lock(tmp_path)
            assert lock_a is lock_b
            other_dir = tmp_path / "other"
            other_dir.mkdir()
            lock_c = file_listener._get_folder_lock(other_dir)
            assert lock_a is not lock_c

STEP D — Add ASYNC-02 concurrency tests:

    class TestFileListenerConcurrentUpload:
        """ASYNC-02: _upload_files runs uploads concurrently via asyncio.gather, not serially."""

        async def test_upload_files_runs_concurrently(self, file_listener, temp_watch_folder):
            """10 files with 0.2s artificial delay each must finish in well under 2s (serial) — around 0.3s."""
            import time
            from unittest.mock import AsyncMock

            async def slow_upload(file_path, s3_key):
                await asyncio.sleep(0.2)
                return True

            file_listener.s3_manager.check_exists = AsyncMock(return_value=False)
            file_listener.s3_manager.upload_file = AsyncMock(side_effect=slow_upload)

            files = [f"f{i}.txt" for i in range(10)]
            for name in files:
                (temp_watch_folder / name).write_text("x")

            start = time.monotonic()
            uploaded = await file_listener._upload_files(files, temp_watch_folder)
            elapsed = time.monotonic() - start

            assert len(uploaded) == 10
            # Serial would be >= 2.0s. Concurrent with semaphore >=10 must be under 1.0s.
            assert elapsed < 1.0, f"Uploads ran serially (elapsed={elapsed:.2f}s)"

        async def test_upload_files_active_tasks_tracked(self, file_listener, temp_watch_folder):
            from unittest.mock import AsyncMock
            file_listener.s3_manager.check_exists = AsyncMock(return_value=False)
            file_listener.s3_manager.upload_file = AsyncMock(return_value=True)

            files = [f"t{i}.txt" for i in range(3)]
            for name in files:
                (temp_watch_folder / name).write_text("x")

            assert len(file_listener._active_upload_tasks) == 0
            await file_listener._upload_files(files, temp_watch_folder)
            # After gather completes, the done_callback has discarded every task.
            assert len(file_listener._active_upload_tasks) == 0

        async def test_upload_files_gather_handles_exceptions(self, file_listener, temp_watch_folder):
            """One raising coroutine must not cancel the rest; return_exceptions=True preserves partial success."""
            from unittest.mock import AsyncMock

            calls = {"n": 0}

            async def maybe_fail(file_path, s3_key):
                calls["n"] += 1
                if calls["n"] == 2:
                    raise RuntimeError("simulated failure")
                return True

            file_listener.s3_manager.check_exists = AsyncMock(return_value=False)
            file_listener.s3_manager.upload_file = AsyncMock(side_effect=maybe_fail)

            files = ["a.txt", "b.txt", "c.txt"]
            for name in files:
                (temp_watch_folder / name).write_text("x")

            uploaded = await file_listener._upload_files(files, temp_watch_folder)
            # Exactly one failed; two succeeded.
            assert len(uploaded) == 2

STEP E — Add IGNORE-03 and IGNORE-04 tests:

    class TestFileListenerIgnoreIntegration:
        """IGNORE-03: FileListener delegates to IGNORE_RULES. IGNORE-04: ignored files increment stats."""

        async def test_file_listener_has_no_local_ignore_attrs(self, file_listener):
            assert not hasattr(file_listener, "ignore_patterns")
            assert not hasattr(file_listener, "ignore_dirs")
            assert not hasattr(file_listener, "_should_ignore_file")
            assert not hasattr(file_listener, "_should_ignore_directory")

        async def test_scan_increments_ignored_files_stat(self, file_listener, temp_watch_folder):
            # Drop a sensitive file that IGNORE_RULES will block
            (temp_watch_folder / ".env").write_text("SECRET=x")
            (temp_watch_folder / "regular.txt").write_text("ok")

            before = file_listener._stats["ignored_files"]
            await file_listener._scan_current_files(temp_watch_folder)
            after = file_listener._stats["ignored_files"]

            assert after - before >= 1  # at least the .env file

STEP F — Adjust any existing test whose assertions depend on the old serial upload loop (e.g., tests that asserted `task.cancel()` was called on timeout). The new `_upload_with_timeout` coroutine handles its own cancellation via `asyncio.wait_for`; no explicit `.cancel()` on a task is needed. If an existing test patched `asyncio.wait_for` on the OLD signature (task as first arg), update it to expect a coroutine as first arg.

STEP G — Run full suite.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_file_listener.py -v --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestFileListenerConfig" tests/unit/test_file_listener.py` returns 1
    - `grep -c "class TestFileListenerAsyncBackupIO" tests/unit/test_file_listener.py` returns 1
    - `grep -c "class TestFileListenerConcurrentUpload" tests/unit/test_file_listener.py` returns 1
    - `grep -c "class TestFileListenerIgnoreIntegration" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_config_max_concurrent_uploads_wires_to_semaphore" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_load_backup_info_uses_aiofiles_and_lock" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_update_backup_info_uses_aiofiles_and_lock" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_folder_lock_is_same_instance_per_folder" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_upload_files_runs_concurrently" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_upload_files_active_tasks_tracked" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_upload_files_gather_handles_exceptions" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_scan_increments_ignored_files_stat" tests/unit/test_file_listener.py` returns 1
    - `grep -c "test_file_listener_has_no_local_ignore_attrs" tests/unit/test_file_listener.py` returns 1
    - `grep -c "file_listener\\._should_ignore_file" tests/unit/test_file_listener.py` returns 0
    - `grep -c "file_listener\\.ignore_patterns" tests/unit/test_file_listener.py` returns 0
    - `uv run pytest tests/unit/test_file_listener.py --no-cov -q` exits 0
    - `uv run pytest --no-cov -q` exits 0 (whole suite green including Plans 01, 02, 03 outputs)
  </acceptance_criteria>
  <done>
    test_file_listener.py contains 9 new behaviour-proving tests, obsolete ignore-method tests removed, all tests pass. Concurrency test elapsed-time assertion proves ASYNC-02 is a real fix (serial would be >2s; concurrent is <1s for 10 x 0.2s uploads).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watched folder → upload semaphore | Every file that passes IGNORE_RULES reaches the upload path; the semaphore caps concurrent S3 puts. Misconfigured or bypassed semaphore can exhaust file descriptors or S3 rate limits. |
| `.milo_backup.info` file on disk | Read-modify-write from both the 5-minute scan and real-time event handlers. Unprotected interleaving loses state (partial writes, re-uploads, MD5-mismatch panic on next scan). |
| Gathered upload task failures | `asyncio.gather(..., return_exceptions=True)` surfaces exceptions as values; a single swallowed exception must not mask information the caller needs for correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | Hardcoded semaphore of 50 ignored `config.max_concurrent_uploads` | mitigate | CONFIG-01: wire `asyncio.Semaphore(self.config.max_concurrent_uploads)` (default 100). Tested by `test_config_max_concurrent_uploads_wires_to_semaphore`. |
| T-04-02 | Tampering (data correctness) | Serial upload loop makes 10 concurrent file changes upload one-at-a-time, dramatically increasing the window where a second change corrupts a partially-uploaded file | mitigate | ASYNC-02: concurrent `asyncio.gather` with per-coroutine `wait_for`. Tested by `test_upload_files_runs_concurrently` (elapsed-time assertion). |
| T-04-03 | Tampering | Blocking `open()` on `.milo_backup.info` during read-modify-write races between scan and event handler; interleaved writes lose state | mitigate | ASYNC-03: `aiofiles.open` + per-folder `asyncio.Lock` serialises access. Tested by `test_load_backup_info_uses_aiofiles_and_lock` and `test_folder_lock_is_same_instance_per_folder`. |
| T-04-04 | Information Disclosure | Dot-files and sensitive files uploaded because `FileListener._should_ignore_file` (the scan path) lacks the dot-file block that `FileChangeHandler._should_ignore_file` has | mitigate | IGNORE-03: delete both local implementations; consume `IGNORE_RULES.should_ignore_file` which unconditionally blocks dot-files and sensitive patterns (see Plan 01 threat T-01-01). Tested by `test_scan_increments_ignored_files_stat`. |
| T-04-05 | Repudiation | `_stats["ignored_files"]` never incremented — operator cannot audit how many files the filter blocked | mitigate | IGNORE-04: increment counter inside `_scan_current_files`. Tested by `test_scan_increments_ignored_files_stat`. |
| T-04-06 | Denial of Service | Lock held for too long could starve other scan cycles | accept | Lock scope is minimal: held only for the `aiofiles` read/write, released before `json.loads`/`json.dumps`. Per-folder locks cap contention to files from the same folder. |
| T-04-07 | Elevation of Privilege | Symlink in watched folder pointing to `~/.ssh/` exposes keys | mitigate | `IGNORE_RULES.should_ignore_dir` (from Plan 01) returns True for symlinked dirs; the recursion guard in `_process_folder_recursively` uses it (EDIT 4). Sensitive filenames inside any accessible directory are blocked by `IGNORE_RULES.should_ignore_file`. |
| T-04-08 | Denial of Service | A single upload task raising uncaught exception in old code would propagate out of the for-loop, leaving sibling tasks orphaned | mitigate | `return_exceptions=True` in gather + per-task try/except in `_upload_with_timeout` isolate failures. Tested by `test_upload_files_gather_handles_exceptions`. |
</threat_model>

<verification>
```bash
# All key behaviours present in code
grep -q "asyncio.Semaphore(self.config.max_concurrent_uploads)" aws_copier/core/file_listener.py
grep -q "self._folder_locks" aws_copier/core/file_listener.py
grep -q "self._active_upload_tasks" aws_copier/core/file_listener.py
grep -q "_upload_with_timeout" aws_copier/core/file_listener.py
grep -q "IGNORE_RULES.should_ignore_file" aws_copier/core/file_listener.py
grep -qE "asyncio\.gather\(\*.*return_exceptions=True" aws_copier/core/file_listener.py

# Deleted code stays deleted
! grep -q "def _should_ignore_file" aws_copier/core/file_listener.py
! grep -q "def _should_ignore_directory" aws_copier/core/file_listener.py
! grep -q "self.ignore_patterns" aws_copier/core/file_listener.py
! grep -q "with open(backup_info_file" aws_copier/core/file_listener.py

# All file_listener tests pass
uv run pytest tests/unit/test_file_listener.py -v --no-cov

# Whole suite still green
uv run pytest --no-cov -q

# Lint + format clean
uv run ruff check aws_copier/core/file_listener.py tests/unit/test_file_listener.py
uv run ruff format --check aws_copier/core/file_listener.py tests/unit/test_file_listener.py
```

All commands exit 0.
</verification>

<success_criteria>
- `upload_semaphore` wired to `config.max_concurrent_uploads` (CONFIG-01 verified)
- `_upload_files` uses `asyncio.gather(*, return_exceptions=True)` with per-coroutine `wait_for` wrapper (ASYNC-02 verified by concurrency test)
- `_load_backup_info` and `_update_backup_info` use `aiofiles.open` under per-folder `asyncio.Lock` (ASYNC-03 verified)
- `self._active_upload_tasks: Set[asyncio.Task]` exists and is populated/discarded correctly (ASYNC-06 hook ready for Plan 05)
- No `asyncio.get_event_loop()` or `asyncio.BaseEventLoop` anywhere in file_listener.py (ASYNC-04)
- `_should_ignore_file`, `_should_ignore_directory`, `ignore_patterns`, `ignore_dirs` all removed (IGNORE-03)
- `_stats["ignored_files"]` increments on each filtered file (IGNORE-04 verified)
- Full test suite remains green (Plans 01, 02, 03, 04 all integrated)
- ruff check + format both pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-reliability/01-04-SUMMARY.md` describing:
- Exact diffs applied to file_listener.py (all 10 edits)
- The new `self._active_upload_tasks` contract (Plan 05 consumer interface)
- Concurrency test elapsed-time results as proof of ASYNC-02 fix
- Test count delta (added ~9, removed ~2)
</output>
